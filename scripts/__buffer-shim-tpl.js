// Browser shim for Node Buffer - generated by fix-esm-extensions.cjs
export function alloc(length) { return new Uint8Array(length); }
export function from(src) { return Uint8Array.from ? Uint8Array.from(src) : new Uint8Array(src); }
export function concat(list) { const n = list.reduce((s,b)=>s+b.length,0); const out = new Uint8Array(n); let o=0; for (const b of list) { out.set(b,o); o+=b.length; } return out; }
export function toString(buf, enc) { return new TextDecoder().decode(buf); }
const Buffer = { alloc, from, concat, toString };
export default Buffer;
export { Buffer };
