## Objective
- Complete Phase 1 of the HYDRA Main AI architecture by implementing all missing core runtime components (audit, sandbox, kill-switch, memory retrieval, billing, evolution), plus fix bugs, clean lint, and ensure all tests pass

## Important Details
- Workspace at `C:\Users\KGN/Desktop\newfilesys`, npm workspaces with `commercial/*`
- Root `vitest.config.ts` includes `commercial/alp-server/tests/**/*.test.js` and `commercial/alp-platform/tests/**/*.test.ts` (but NOT hydra-main tests)
- hydra-main has its own `vitest.config.ts` including `tests/**/*.test.ts`
- Python package `alp-portable/HYDRA` installed via `pip install -e .` (setuptools, `where = ["python"]`)
- Python linting: ruff (line-length=100, target py310)
- Rust workspace at `alp-portable/HYDRA/Cargo.toml`
- `commercial/hydra-main/src/tools/registry.ts` defines `Tool` interface: id, name, description, category, requiredCapabilities, execute(input)
- Persona tool IDs must match tool implementation IDs (e.g., "testing-unit" not "unit-test")

## Work State
### Completed
- **Phase 1 HYDRA Core — all 9 components implemented**:
  - `core/orchestrator.ts` — Task lifecycle, agent dispatch, event bus, retry logic
  - `core/scheduler.ts` — Priority queue, concurrency limits, resource budgets
  - `core/context-engine.ts` — Project graph, dependency resolution, state sync
  - `core/evolution.ts` — Skill extraction, benchmarking, candidate promotion with usage tracking, success rates, latency metrics, `score()`, `evaluateCandidates`, `promoteCandidate`, `benchmarkCandidate`, `pruneOldCandidates`
  - `agents/registry.ts` — 14 agent personas (Engineer, Security, DevOps, Data, EDA, Quantum, SOC, ThreatIntel, ZeroTrust, TestEngine, CodeReview, ReleaseManager, APIDesigner, ArchitectureVisualizer)
  - `models/router.ts` — ModelRouter with 7 providers (OpenAI, Anthropic, Azure, AWS, GCP, Ollama, Local), failover, cost-aware routing
  - `memory/store.ts` + `memory/stores/*` — Session, project, agent, global, episodic stores with factory
  - `memory/retrieval.ts` — MemoryRetriever with semantic/keyword/temporal/hybrid search + MockEmbeddingProvider
  - `security/kernel.ts` — SecurityKernel with capability-based auth
  - `security/audit.ts` — AuditLogger with SHA-256 chained hash ledger, tamper detection, event query
  - `security/sandbox.ts` — SandboxManager with dynamic sandbox creation, resource limits, network policy enforcement, pause/resume
  - `security/kill-switch.ts` — KillSwitch with global/org/agent/task scope emergency stop + `emergencyKillSwitch` singleton
  - `tenant/organization.ts` — TenantManager (org/team/user CRUD)
  - `tenant/billing.ts` — BillingManager with plans, usage tracking, invoice generation, markPaid
  - `tools/implementations.ts` — 66 tool implementations across 10 categories
  - `tools/registry.ts` — ToolRegistry
  - `verification/quality-gates.ts` — QualityGateEngine

- **Integration updates**:
  - `index.ts` — exports all 6 new modules (AuditLogger, SandboxManager, KillSwitch, BillingManager, MemoryRetriever, EvolutionEngine)
  - `server.ts` — added routes for /sandboxes, /audit, /killswitch, /billing/usage, /billing/invoices, /billing/subscription, /memory/search, /evolution/candidates, /evolution/evaluations, /evolution/promote
  - `alp-server/routes/hydra.ts` — updated to import and expose all 6 new components with matching routes
  - `alp-platform/src/index.ts` — added `hydra.audit`, `hydra.sandbox`, `hydra.killSwitch`, `hydra.billing`, `hydra.memory`, `hydra.evolution` to the `hydra` property (aliased hydra-main `AuditLogger` as `HydraAuditLogger` to avoid conflict with platform's own AuditLogger)
  - Rebuilt `dist/` for hydra-main, alp-server, alp-platform

- **Bug fixes**:
  - `router.ts` `completeWithFallback`: Added config check to skip unconfigured providers
  - `mapping.ts` tool IDs: Fixed 8 mismatched IDs
  - Python ruff: Fixed 22 errors (17 unused imports, 2 missing imports)
  - ESLint: Cleaned 25 warnings (unused imports, unused vars, unused catch params)

- **All tests pass**: hydra-main: 138 (105 + 33), root: 971, Python: 6, Rust: 58 — total 1093
- **TypeScript typecheck clean**: hydra-main, alp-platform, alp-server
- **ESLint clean**: 0 warnings, 0 errors for hydra-main

### Testing Results (All Green)
- **TypeScript (root vitest)**: 971 tests passed | 0 failed | 150 test files
- **hydra-main (vitest)**: 138 tests passed | 0 failed | 2 test files (fixed dup import in hydra-main.test.ts)
- **alp-server (vitest)**: 33 tests passed (via root config)
- **Python (pytest)**: 6 tests passed | 0 failed
- **Rust (cargo test --workspace)**: 58 tests passed | 0 failed | 24 crates
- **ESLint**: hydra-main `eslint src --ext .ts` — 0 errors
- **TSDump (tsc --noEmit)**: hydra-main, alp-server, alp-platform — 0 errors
- **Python ruff**: All checks passed (fixed 1 import-sort issue in __main__.py)
- **Python mypy**: Success — no issues found in 8 source files
- **Rust clippy**: 0 warnings (1 expected `mobile` cfg warning in tauri)

### Active
- (none — all work complete and verified)

### Blocked
- (none)

## Next Move
- Phase 2 (Agent Personas) through Phase 8 (Ecosystem) from `docs/AI_ARCHITECTURE.md` are future product/UI phases (Q1 2027+). Phase 1 HYDRA Core is fully implemented.

## Relevant Files
- `commercial/hydra-main/src/core/evolution.ts` — EvolutionEngine: recordSkillUsage, evaluateCandidates, promoteCandidate, benchmarkCandidate, extractSkillsFromPersona, pruneOldCandidates
- `commercial/hydra-main/src/security/audit.ts` — AuditLogger: log, verify (tamper detection via SHA-256 chained hashes), query
- `commercial/hydra-main/src/security/sandbox.ts` — SandboxManager: create, destroy, pause, resume, checkResources, checkNetworkAccess, updateResourceUsage
- `commercial/hydra-main/src/security/kill-switch.ts` — KillSwitch: activate, deactivate, shouldBlock, isAllKillsActive, history
- `commercial/hydra-main/src/memory/retrieval.ts` — MemoryRetriever: search, searchSemantic, searchKeyword, searchTemporal, hybridSearch; MockEmbeddingProvider
- `commercial/hydra-main/src/tenant/billing.ts` — BillingManager: addPlan, subscribeOrganization, recordUsage, generateInvoice, markPaid
- `commercial/hydra-main/src/index.ts` — Updated to export all 6 new modules
- `commercial/hydra-main/src/server.ts` — Added routes: /sandboxes, /audit, /killswitch, /billing/usage, /billing/invoices, /billing/subscription, /memory/search, /evolution/candidates, /evolution/evaluations, /evolution/promote
- `commercial/hydra-main/src/models/router.ts` — ModelRouter with fixed completeWithFallback
- `commercial/hydra-main/src/products/mapping.ts` — 24 product configs with corrected tool IDs
- `commercial/hydra-main/src/products/workflows.ts` — 19 product workflows with validated tool/agent references
- `commercial/hydra-main/src/tools/implementations.ts` — 66 tool classes
- `commercial/hydra-main/src/agents/registry.ts` — 14 personas + AgentRegistry + PersonaBuilder
- `commercial/hydra-main/src/agents/base.ts` — AgentPersona interface with rich metadata
- `commercial/hydra-main/src/server.ts` — Express API server with all routes
- `commercial/hydra-main/.eslintrc.json` — ESLint config
- `commercial/hydra-main/tests/hydra-main.test.ts` — 105 tests (was 83, added 22 new tests for 6 new components)